# Distressed Asset Radar V0.1

Local-first Next.js MVP for capturing, normalizing, scoring, deduplicating, reviewing, watchlisting, and tracking distressed asset opportunities. The app persists to `data/db.json` for local development and includes Supabase-compatible migrations under `supabase/migrations`.
