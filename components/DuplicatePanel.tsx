import Link from 'next/link';export default function DuplicatePanel({items}:{items:any[]}){return <section className="card"><h3 className="text-lg font-bold">Duplicate Candidates</h3>{items.length===0?<p>No duplicate candidates.</p>:items.map(d=><div className="rounded border p-2" key={d.id}><p><b>{d.status}</b> score {d.duplicateScore}</p><p>Candidate: {d.candidate?<Link className="text-blue-700" href={`/signals/${d.candidate.id}`}>{d.candidate.title}</Link>:'Unknown'}</p><p>Duplicate reasons: {(d.reasons||[]).join(', ')}</p></div>)}</section>}
