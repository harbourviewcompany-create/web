'use client';import {useRouter} from 'next/navigation';export default function OutcomeForm({id,current}:{id:string;current:string}){const r=useRouter();return <form className="card space-y-2" onSubmit={async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);await fetch(`/api/signals/${id}/outcome`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd))});r.refresh();}}><h3 className="text-lg font-bold">Outcome Tracker</h3><select name="status" className="input" defaultValue={current}><option>ignored</option><option>reviewed</option><option>pursued</option><option>contacted</option><option>bid</option><option>won</option><option>lost</option><option>expired</option></select><input name="amount" className="input" placeholder="Bid/amount" type="number"/><input name="nextAction" className="input" placeholder="Next action"/><textarea name="notes" className="input" placeholder="Notes / response"/><button className="btn">Save outcome</button></form>}
