'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function ManualUrlForm(){const[busy,setBusy]=useState(false),[err,setErr]=useState('');const r=useRouter();return <form className="card max-w-2xl space-y-3" onSubmit={async e=>{e.preventDefault();setBusy(true);setErr('');const fd=new FormData(e.currentTarget);const res=await fetch('/api/signals/manual-url',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd))});const j=await res.json();setBusy(false);if(!res.ok)setErr(j.error||'Failed');else r.push(`/signals/${j.record.id}`)}}><label className="label">URL</label><input name="url" className="input" placeholder="https://example.com/asset-auction" required/><label className="label">Source name</label><input name="sourceName" className="input"/><label className="label">Source type</label><select name="sourceType" className="input"><option value="unknown">unknown</option><option value="auction">auction</option><option value="government_surplus">government_surplus</option><option value="liquidation">liquidation</option></select><button className="btn" disabled={busy}>{busy?'Capturing...':'Capture URL'}</button>{err&&<p className="text-red-700">{err}</p>}</form>}
