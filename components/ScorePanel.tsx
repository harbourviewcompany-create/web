export default function ScorePanel({score,explanations}:{score:any;explanations:any[]}){return <section className="card"><h3 className="text-lg font-bold">Opportunity Score Panel</h3><p className="text-3xl font-black">{score?.totalScore} <span className="text-base font-normal">{score?.scoreBand}</span></p><p>Scoring rule version: {score?.scoringRuleVersion}</p><div className="mt-2 grid grid-cols-2 gap-2">{score&&Object.entries(score.components).map(([k,v])=><div className="rounded bg-slate-100 p-2" key={k}>{k}: <b>{String(v)}</b></div>)}</div><h4 className="mt-3 font-bold">Component explanations</h4><ul className="list-disc pl-5">{explanations.map(e=><li key={e.id}>{e.component}: {e.explanation}</li>)}</ul></section>}
