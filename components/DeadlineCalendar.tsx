export default function DeadlineCalendar({items}:{items:any[]}){const groups=items.reduce((a:any,d)=>{const k=d.deadlineAt?new Date(d.deadlineAt).toLocaleDateString():'Unknown';(a[k]??=[]).push(d);return a},{});return <div className="space-y-3">{Object.entries(groups).map(([day,rows]:any)=><section className="card" key={day}><h3 className="font-bold">{day}</h3>{rows.map((r:any)=><p key={r.id}><span className="badge">{r.deadlineStatus}</span> {r.signal?.title}</p>)}</section>)}</div>}
