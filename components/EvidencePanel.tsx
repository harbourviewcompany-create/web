export default function EvidencePanel({snapshot,evidence}:{snapshot:any;evidence:any[]}){return <section className="card"><h3 className="text-lg font-bold">Evidence Panel</h3><p>Original URL: {snapshot?.originalUrl||'—'}</p><p>Snapshot timestamp: {snapshot?.capturedAt||'—'}</p><p>Content hash: <code className="text-xs">{snapshot?.contentHash}</code></p><p className="mt-2 rounded bg-slate-50 p-2">Captured text excerpt: {(snapshot?.capturedText||'').slice(0,400)}</p><ul className="mt-2 list-disc pl-5">{evidence.map(e=><li key={e.id}>{e.label}: {e.value} — {e.sourceTextExcerpt?.slice(0,160)}</li>)}</ul></section>}
