import {describe,expect,it} from 'vitest';import {scoreOpportunity} from '@/lib/scoring/scoreOpportunity';
const future=new Date(Date.now()+8*86400000).toISOString();const past=new Date(Date.now()-86400000).toISOString();
describe('scoring',()=>{it('high-value future auction scores strong/excellent',()=>{const s=scoreOpportunity({estimatedValue:185000,expectedResaleValue:300000,sourceType:'auction',assetType:'machinery',location:'Buffalo, NY',deadlineAt:future,deadlineStatus:'confirmed',deadlineConfidence:'high',hasSnapshot:true,confidenceHighCount:5});expect(s.totalScore).toBeGreaterThanOrEqual(75)});it('missing deadline creates penalty',()=>{const s=scoreOpportunity({estimatedValue:10000,sourceType:'unknown',assetType:'unknown'});expect(s.components.missingDataPenalty).toBeGreaterThanOrEqual(5)});it('duplicate creates penalty',()=>{const s=scoreOpportunity({estimatedValue:50000,sourceType:'auction',assetType:'equipment',duplicateStatus:'possible_duplicate',duplicateScore:.9});expect(s.components.duplicatePenalty).toBe(15)});it('expired deadline returns low score or needs_review',()=>{const s=scoreOpportunity({estimatedValue:10000,sourceType:'auction',assetType:'vehicles',deadlineAt:past,deadlineStatus:'expired',criticalReviewDefect:true});expect(['needs_review','ignore','weak']).toContain(s.scoreBand)})})
