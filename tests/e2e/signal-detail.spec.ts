import {test,expect} from '@playwright/test';test('detail score and evidence panels appear',async({page})=>{await page.goto('/signals/sig-cnc');await expect(page.getByText('Opportunity Score Panel')).toBeVisible();await expect(page.getByText('Evidence Panel')).toBeVisible();await expect(page.getByText('Content hash:')).toBeVisible();await expect(page.getByText('Component explanations')).toBeVisible();});test('filters work',async({page})=>{await page.goto('/signals?assetType=machinery');await expect(page.getByText('CNC Machine Shop Equipment Auction').first()).toBeVisible();await expect(page.getByText('2019 Ford F-550')).toHaveCount(0);});
