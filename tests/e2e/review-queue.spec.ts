import {test,expect} from '@playwright/test';test('review queue and duplicate candidate visible',async({page})=>{await page.goto('/review');await expect(page.getByText('Assets available soon')).toBeVisible();await expect(page.getByText(/unknown|low/)).toBeVisible();await expect(page.getByText('possible_duplicate')).toBeVisible();await expect(page.getByText('Duplicate reasons:')).toBeVisible();});
