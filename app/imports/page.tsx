import {getDb} from '@/lib/store/db';export default function Imports(){return <><h2 className="mb-3 text-2xl font-bold">Import Batch History</h2><div className="card"><table className="w-full text-sm"><tbody>{getDb().importBatches.map(b=><tr className="border-t" key={b.id}><td>{b.filename}</td><td>rows {b.rowCount}</td><td>valid {b.validCount}</td><td>invalid {b.invalidCount}</td><td>{b.status}</td><td>{b.createdAt}</td></tr>)}</tbody></table></div></>}
