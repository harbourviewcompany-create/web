import {NextResponse} from 'next/server';import {OutcomeUpdateSchema} from '@/lib/schemas/outcome';import {addOutcome} from '@/lib/store/signals';export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; const parsed=OutcomeUpdateSchema.safeParse(await req.json()); if(!parsed.success)return NextResponse.json({error:parsed.error.message},{status:400}); return NextResponse.json(addOutcome(id,parsed.data));}
