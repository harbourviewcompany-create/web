import {NextResponse} from 'next/server';import {updateReview} from '@/lib/store/signals';export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; const body=await req.json(); return NextResponse.json(updateReview(id,body.status));}
