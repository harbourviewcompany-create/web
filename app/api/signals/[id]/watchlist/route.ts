import {NextResponse} from 'next/server';import {setWatchlist} from '@/lib/store/signals';export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; let body={notes:''}; try{body=await req.json()}catch{} return NextResponse.json(setWatchlist(id,true,body.notes));}export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; return NextResponse.json(setWatchlist(id,false));}
