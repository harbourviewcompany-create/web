import {NextResponse} from 'next/server';import {ManualUrlInputSchema} from '@/lib/schemas/signal';import {createManualUrl} from '@/lib/store/signals';export async function POST(req:Request){const parsed=ManualUrlInputSchema.safeParse(await req.json()); if(!parsed.success)return NextResponse.json({error:parsed.error.message},{status:400}); return NextResponse.json(await createManualUrl(parsed.data));}
