export function normalizeUrl(input:string){const u=new URL(input);u.hostname=u.hostname.toLowerCase();u.hash='';const pairs:Array<[string,string]>=[];u.searchParams.forEach((v,k)=>{if(!k.toLowerCase().startsWith('utm_'))pairs.push([k,v])});pairs.sort(([a],[b])=>a.localeCompare(b));u.search='';for(const [k,v] of pairs)u.searchParams.append(k,v);let s=u.toString();if(s.endsWith('/'))s=s.slice(0,-1);return s;}
